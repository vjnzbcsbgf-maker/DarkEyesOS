# DarkEyesOS: allow the live user to enable admin (set the sudo password) once,
# without already having sudo. This one command only — not a general root grant.
amnesia ALL=(root) NOPASSWD: /usr/local/bin/darkeyes-setadmin
